services:
  mycat:
    build:
      context: .
    image: mycat:local
    container_name: mycat-app-windows
    pull_policy: never

    environment:
      - QT_QPA_PLATFORM=xcb
      - DISPLAY=host.docker.internal:0.0
      - LIBGL_ALWAYS_INDIRECT=1

    volumes: []
    restart: "no"